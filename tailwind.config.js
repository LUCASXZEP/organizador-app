/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Ajuste para o seu projeto
  darkMode: 'class', // ativar modo escuro via classe 'dark' no root
  theme: {
    extend: {},
  },
  plugins: [],
}
